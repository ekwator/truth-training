# Contracts Overview

| Contract | Purpose |
|----------|---------|
| `doc_refactor_cli.md` | Canonical CLI contract for running documentation refactor phases, describing inputs, outputs, and exit codes. |

Consumers: release engineers, CI maintainers, automation agents.

All contracts assume the repository root `/home/ekwator/Code/truth-training` and Python virtualenv availability.
