# truth-training Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-10-20

## Active Technologies
- Rust 1.75+ (existing project) + Actix-web, SQLite, ed25519_dalek, serde, tokio (001-collective-intelligence-layer)
- Rust 1.75+ (Tauri), TypeScript/JavaScript (frontend) + Tauri, React/Vue (frontend framework), HTTP client for API communication (002-ui-desktop-integration)
- Local SQLite via core system API (no direct database access) (002-ui-desktop-integration)
- Rust 1.78+ (Tauri backend), TypeScript (Tauri frontend) + Tauri, SQLite (rusqlite), simple TS UI (text-only) (003-truth-training-desktop)
- SQLite (local, offline-first) (003-truth-training-desktop)
- Rust (stable), TypeScript/React (desktop UI), SQLite (embedded DB) + Actix Web (server API), Tauri (desktop UI), rusqlite (storage), React/TypeScript (UI) (006-context-fields-embedded)
- SQLite embedded database (local persistence) (006-context-fields-embedded)
- Kotlin 2.0.20, Android SDK API 24+ (target 35), JDK 17 + Room 2.6.1, Retrofit 2.11.0, OkHttp 4.12.0, WorkManager 2.9.0, Coroutines 1.9.0, BouncyCastle 1.78 (Ed25519) (007-title-align-truth)
- Room (SQLite) database with WAL mode, migrating from SharedPreferences (007-title-align-truth)
- Rust 1.75+ (CLI, Server, Desktop core), Kotlin (Android), TypeScript (Desktop UI), SQLite 3.x (008-specify-md)
- SQLite (embedded) with Room (Android), rusqlite (Rust modules) (008-specify-md)
- Rust 1.75+ (CLI/Server/Desktop core), Kotlin (Android), TypeScript/React (Desktop UI), SQLite 3.x + `rusqlite`, `tokio`, `actix-web`, `reqwest`, `clap`, `ed25519-dalek`, Room (Android), WorkManager, Electron/Tauri runtime (008-specify-md)
- SQLite (shared) with Room entities on Android and rusqlite elsewhere (008-specify-md)
- Rust 1.75+ (stable toolchain) (009-fix-linux-deb)
- N/A (packaging only, no data storage changes) (009-fix-linux-deb)
- Python 3.11 (documentation refactor tooling), Rust stable (core project), Node.js 20+ (desktop tooling), Bash (automation scripts) + Spec-Kit (`.specify`), `scripts/doc_refactor` Python package, Git, Markdown parsers, existing GitHub Actions/CI jobs (001-docs-refactor)
- File system (Markdown files, JSON/Markdown reports); no new database storage introduced (001-docs-refactor)

## Project Structure
```
src/
tests/
```

## Commands
cargo test [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] cargo clippy

## Code Style
Rust 1.75+ (existing project): Follow standard conventions

## Recent Changes
- 001-docs-refactor: Added Python 3.11 (documentation refactor tooling), Rust stable (core project), Node.js 20+ (desktop tooling), Bash (automation scripts) + Spec-Kit (`.specify`), `scripts/doc_refactor` Python package, Git, Markdown parsers, existing GitHub Actions/CI jobs
- 009-fix-linux-deb: Added Rust 1.75+ (stable toolchain)
- 009-fix-linux-deb: Added Rust 1.75+ (stable toolchain)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
